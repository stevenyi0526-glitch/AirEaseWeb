# AirEase Web Frontend Environment Configuration
# Copy this file to .env.local and fill in your values
# ⚠️ NEVER commit .env.local to version control!

# =============================================================
# Backend API Configuration
# =============================================================

# Backend API URL (default: http://localhost:8000)
VITE_API_URL=http://localhost:8000

# =============================================================
# Notes:
# =============================================================
# - All VITE_ prefixed variables are exposed to the browser
# - Do NOT store API keys here — all API keys belong in backend/.env
# - Gemini, SerpAPI, Amadeus keys are all handled by the backend
# - The frontend only talks to the backend, never directly to external APIs
